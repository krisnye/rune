import { describe } from "riteway";

describe("tictactoe sample", async (assert) => {
  assert({
    given: "the sample package setup",
    should: "run basic riteway assertions",
    actual: true,
    expected: true
  });
});
